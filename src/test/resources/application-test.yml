spring:
  datasource:
    url: jdbc:h2:mem:authdb;MODE=PostgreSQL
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

sms:
  provider: mock
  api-key: test-key
  api-secret: test-secret
  from-number: 01012345678

# 테스트 컨텍스트에서 월배치 스케줄러가 뜨면 order.api 를 부르려 한다.
member-grade:
  scheduler:
    enabled: false
